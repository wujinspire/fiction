Paste article, translate it, then update the index.
In fiction indexes, keep numbered stories in reverse order.
`novella` is always the first non-`new` item.
Use `scripts/translate.py` with the configured Gemini API for translations; do not translate manually.
When adding stories, update both English and Chinese indexes and sidebars.
Mark first-person or viewpoint-led stories with the `pov` badge in `StoryFeatures`.
Do not combine `new` with `pov`; keep `new` available for future non-POV highlights.
